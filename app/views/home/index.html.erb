<%
schedules = {}
    User.all.each{ |user|
    if !user.schedule.nil? && !user.schedule.id.nil?
    schedules[user.schedule.id.to_s] = user.schedule
    end
    }

 %>
<%= schedules.keys %>

<% User.all.each {|s| s.avatar.reprocess! } %>
<p> lol </p>